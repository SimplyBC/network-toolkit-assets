# Contributing

Start with docs/AUTHORING.md. Keep changes to specific device families and include synthetic fixtures for output changes. Report the real product/version and command semantics without publishing customer outputs. CI is read-only for pull requests; only the manual main-branch release workflow can sign/publish. The toolkit's collectors remain read-only and a device match must not imply unproven credentials, capabilities, reachability or cabling.
